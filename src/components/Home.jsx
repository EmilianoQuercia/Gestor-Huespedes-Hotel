import React from 'react';
import Formulario from './Formulario';
import Header from './Header';

const Home = () => {
    return (
        <div>
            <Header/>
            <Formulario/>   
        </div>
    );
}

export default Home;
